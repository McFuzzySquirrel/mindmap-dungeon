# UI

Presentation and interactions belong here.

Allowed imports:
- @ui/*
- @features/*
- @core/* (read-only view models)

Disallowed imports:
- Direct filesystem access outside @services/* contracts
